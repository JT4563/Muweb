# RabbitMQ Configuration File

# ========================================
# NETWORK & CLUSTERING
# ========================================
listeners.tcp.default = 5672
management.tcp.port = 15672

# ========================================
# MEMORY
# ========================================
vm_memory_high_watermark.relative = 0.6
vm_memory_high_watermark_paging_ratio = 0.75
memory_monitor_interval = 2500

# ========================================
# DISK SPACE
# ========================================
disk_free_limit.absolute = 50MB

# ========================================
# QUEUE MASTER LOCATION
# ========================================
queue_master_locator = min-masters

# ========================================
# MESSAGE TTL & EXPIRATION
# ========================================
# Channel parameters
channel_max = 2048
connection_max = infinity

# ========================================
# HEARTBEAT
# ========================================
heartbeat = 60

# ========================================
# MANAGEMENT PLUGIN
# ========================================
## management.load_definitions = /etc/rabbitmq/definitions.json
## definitions file not provided in this repository by default; keep disabled

# ========================================
# LOGGING
# ========================================
log.file.level = info
log.console = true
log.console.level = info

# ========================================
# SSL/TLS (if needed)
# ========================================
# ssl_options.certfile   = /etc/rabbitmq/server_certificate.pem
# ssl_options.keyfile    = /etc/rabbitmq/server_key.pem
# ssl_options.cacertfile = /etc/rabbitmq/ca_certificate.pem
# ssl_options.verify     = verify_peer
# ssl_options.fail_if_no_peer_cert = false

# ========================================
# PLUGINS
# ========================================
# plugins.dir is not supported in this RabbitMQ image/config version
# If you have custom plugins, place them in the container's plugins folder
# or use the image's recommended mechanism. Removing this line prevents
# startup failures due to unknown configuration keys.

# ========================================
# FRAME MAX (max message size)
# ========================================
frame_max = 131072

# ========================================
# COLLECT STATISTICS
# ========================================
collect_statistics_interval = 5000

# ========================================
# QUEUE DEFAULTS
# ========================================
# Transient or persistent (default: transient)
# durable = true

# ========================================
# MANAGEMENT DATABASE
# ========================================
mnesia_table_loading_retry_timeout = 30000
mnesia_table_loading_retry_limit = 10

# ========================================
# PERMISSIONS
# ========================================
# Default user/pass: guest/guest
default_user = guest
default_pass = guest
default_permissions.configure = .*
default_permissions.read = .*
default_permissions.write = .*
